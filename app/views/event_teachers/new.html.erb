<%= form_for [@school, @event, @teacher_event] do |f| %>

<% options = options_from_collection_for_select(@teachers, 'id', 'first_name') %>
<%= f.select :teacher_id, options %>

<%= f.submit "submit" %>
<% end %>
