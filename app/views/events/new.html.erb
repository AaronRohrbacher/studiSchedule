<%= form_for [@school, @event] do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_field :description %>

<%= f.label :event_type %>
<%= f.select :event_type, [['Class', 'class'],['Private Lesson', 'private']] %>


<%= f.submit "submit" %>
<% end %>
